# Forced Commitment Mode

The game autosaves the moment anything risky happens - raids, downed or killed pawns, mental breaks, fires and more - so no outcome can be reloaded away, and ALT+F4 cannot dodge a save. Every trigger is togglable, all on by default; while a commitment save is running, all debug buttons except the log window are hidden.

Only works in commitment mode saves.
